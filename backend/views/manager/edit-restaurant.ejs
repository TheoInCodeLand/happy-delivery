<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Restaurant - Restaurant Manager</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <style>
        :root {
            --primary-color: #4a6fa5;
            --secondary-color: #166088;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --info-color: #17a2b8;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
        }
        
        /* Header */
        .page-header {
            background: white;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            margin-bottom: 30px;
            border-left: 5px solid var(--primary-color);
        }
        
        .page-header h2 {
            margin: 0;
            color: var(--primary-color);
            font-weight: 700;
        }
        
        .page-header p {
            margin: 5px 0 0 0;
            color: #666;
            font-size: 0.95rem;
        }
        
        /* Restaurant Info Card */
        .restaurant-info-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            padding: 25px;
            margin-bottom: 30px;
            border-top: 4px solid var(--primary-color);
        }
        
        .restaurant-image {
            width: 120px;
            height: 120px;
            border-radius: 12px;
            object-fit: cover;
            margin-right: 25px;
            border: 3px solid white;
            box-shadow: 0 3px 15px rgba(0,0,0,0.1);
        }
        
        .restaurant-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
            margin-right: 8px;
            margin-bottom: 8px;
        }
        
        .badge-active {
            background: rgba(40, 167, 69, 0.1);
            color: var(--success-color);
            border: 1px solid var(--success-color);
        }
        
        .badge-inactive {
            background: rgba(220, 53, 69, 0.1);
            color: var(--danger-color);
            border: 1px solid var(--danger-color);
        }
        
        .badge-verified {
            background: rgba(23, 162, 184, 0.1);
            color: var(--info-color);
            border: 1px solid var(--info-color);
        }
        
        .badge-featured {
            background: rgba(255, 193, 7, 0.1);
            color: var(--warning-color);
            border: 1px solid var(--warning-color);
        }
        
        /* Stats */
        .restaurant-stats {
            display: flex;
            gap: 30px;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #eee;
        }
        
        .stat-item {
            text-align: center;
        }
        
        .stat-value {
            display: block;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--primary-color);
            line-height: 1;
        }
        
        .stat-label {
            font-size: 0.85rem;
            color: #666;
            margin-top: 5px;
        }
        
        /* Form Container */
        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            padding: 30px;
            margin-bottom: 30px;
        }
        
        /* Tabs */
        .nav-tabs {
            border-bottom: 2px solid #dee2e6;
            margin-bottom: 25px;
        }
        
        .nav-tabs .nav-link {
            border: none;
            color: #666;
            font-weight: 500;
            padding: 12px 25px;
            border-radius: 8px 8px 0 0;
            margin-right: 5px;
            transition: all 0.3s;
        }
        
        .nav-tabs .nav-link:hover {
            color: var(--primary-color);
            background-color: rgba(74, 111, 165, 0.05);
        }
        
        .nav-tabs .nav-link.active {
            color: var(--primary-color);
            background-color: white;
            border-bottom: 3px solid var(--primary-color);
        }
        
        /* Form Elements */
        .form-label {
            font-weight: 600;
            color: #555;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .form-label .required {
            color: var(--danger-color);
            font-size: 0.8rem;
        }
        
        .form-control, .form-select {
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 12px 15px;
            transition: all 0.3s;
            font-size: 0.95rem;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(74, 111, 165, 0.15);
        }
        
        .form-control.is-invalid {
            border-color: var(--danger-color);
        }
        
        .invalid-feedback {
            color: var(--danger-color);
            font-size: 0.85rem;
            margin-top: 5px;
        }
        
        /* Image Upload */
        .image-upload-container {
            border: 2px dashed #ddd;
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
            background-color: #f8f9fa;
            position: relative;
            min-height: 200px;
        }
        
        .image-upload-container:hover {
            border-color: var(--primary-color);
            background-color: rgba(74, 111, 165, 0.05);
        }
        
        .image-upload-container.dragover {
            border-color: var(--success-color);
            background-color: rgba(40, 167, 69, 0.05);
        }
        
        .image-preview {
            max-width: 100%;
            max-height: 200px;
            border-radius: 10px;
            object-fit: cover;
            margin: 15px auto;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }
        
        .image-actions {
            position: absolute;
            top: 10px;
            right: 10px;
            display: flex;
            gap: 8px;
        }
        
        .image-action-btn {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: none;
            background: rgba(0,0,0,0.7);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .image-action-btn:hover {
            background: rgba(0,0,0,0.9);
            transform: scale(1.1);
        }
        
        /* Opening Hours */
        .hours-container {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
        }
        
        .day-hours {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
            padding: 10px;
            background: white;
            border-radius: 8px;
        }
        
        .day-label {
            min-width: 120px;
            font-weight: 600;
            color: #555;
        }
        
        /* Toggle Switches */
        .form-check-input {
            width: 2.5em;
            height: 1.25em;
        }
        
        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        /* Map Preview */
        .map-preview {
            height: 300px;
            border-radius: 10px;
            overflow: hidden;
            border: 2px solid #e0e0e0;
        }
        
        /* Buttons */
        .btn {
            padding: 12px 30px;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            border: none;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(74, 111, 165, 0.3);
        }
        
        .btn-danger {
            background: linear-gradient(135deg, var(--danger-color) 0%, #c82333 100%);
            border: none;
        }
        
        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(220, 53, 69, 0.3);
        }
        
        .btn-secondary {
            border: 2px solid #ddd;
        }
        
        /* Loading Spinner */
        .spinner-container {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255,255,255,0.9);
            z-index: 9999;
            justify-content: center;
            align-items: center;
        }
        
        .spinner {
            width: 50px;
            height: 50px;
            border: 5px solid #f3f3f3;
            border-top: 5px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* Toast Notification */
        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }
        
        .toast {
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.15);
            border: none;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
            
            .restaurant-stats {
                flex-direction: column;
                gap: 15px;
            }
            
            .image-upload-container {
                min-height: 150px;
            }
            
            .btn {
                width: 100%;
                margin-bottom: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="spinner-container" id="loadingSpinner">
        <div class="spinner"></div>
    </div>
    
    <div class="container py-4">
        <!-- Header -->
        <div class="page-header">
            <div class="d-flex justify-content-between align-items-start">
                <div>
                    <h2><i class="fas fa-edit me-2"></i> Edit Restaurant</h2>
                    <p>Update restaurant details and settings</p>
                </div>
                <div>
                    <a href="/manager/restaurants" class="btn btn-outline-secondary me-2">
                        <i class="fas fa-arrow-left me-1"></i> Back to Restaurants
                    </a>
                    <button class="btn btn-danger" onclick="showDeleteConfirmation()">
                        <i class="fas fa-trash me-1"></i> Delete Restaurant
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Restaurant Info -->
        <div class="restaurant-info-card">
            <div class="d-flex align-items-start">
                <img id="currentLogo" class="restaurant-image" src="<%= restaurant.logo_url || 'https://images.unsplash.com/photo-1517248135467-4c7edcad34c4?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80' %>" 
                     alt="<%= restaurant.name %>">
                <div class="flex-grow-1">
                    <h3 id="currentName"><%= restaurant.name %></h3>
                    <p class="text-muted" id="currentDescription"><%= restaurant.description || 'No description' %></p>
                    
                    <div class="mb-3">
                        <% if (restaurant.is_active) { %>
                            <span class="restaurant-badge badge-active">
                                <i class="fas fa-check-circle me-1"></i> Active
                            </span>
                        <% } else { %>
                            <span class="restaurant-badge badge-inactive">
                                <i class="fas fa-times-circle me-1"></i> Inactive
                            </span>
                        <% } %>
                        
                        <% if (restaurant.is_verified) { %>
                            <span class="restaurant-badge badge-verified">
                                <i class="fas fa-badge-check me-1"></i> Verified
                            </span>
                        <% } %>
                        
                        <% if (restaurant.is_featured) { %>
                            <span class="restaurant-badge badge-featured">
                                <i class="fas fa-star me-1"></i> Featured
                            </span>
                        <% } %>
                        
                        <span class="restaurant-badge" style="background: rgba(74, 111, 165, 0.1); color: var(--primary-color); border: 1px solid var(--primary-color);">
                            <i class="fas fa-utensils me-1"></i> <%= restaurant.cuisine_type || 'Multi-cuisine' %>
                        </span>
                    </div>
                    
                    <p class="mb-1">
                        <i class="fas fa-map-marker-alt text-primary me-2"></i>
                        <span id="currentAddress"><%= restaurant.address %></span>
                    </p>
                    <p class="mb-1">
                        <i class="fas fa-phone text-primary me-2"></i>
                        <span id="currentPhone"><%= restaurant.phone_number %></span>
                    </p>
                </div>
            </div>
            
            <div class="restaurant-stats">
                <div class="stat-item">
                    <span class="stat-value" id="totalOrders"><%= restaurant.total_orders || '0' %></span>
                    <span class="stat-label">Total Orders</span>
                </div>
                <div class="stat-item">
                    <span class="stat-value">₹<span id="totalRevenue"><%= restaurant.total_revenue || '0' %></span></span>
                    <span class="stat-label">Total Revenue</span>
                </div>
                <div class="stat-item">
                    <span class="stat-value" id="avgRating"><%= restaurant.average_rating || '0.0' %></span>
                    <span class="stat-label">Average Rating</span>
                </div>
                <div class="stat-item">
                    <span class="stat-value" id="activeOrders"><%= restaurant.active_orders || '0' %></span>
                    <span class="stat-label">Active Orders</span>
                </div>
            </div>
        </div>
        
        <!-- Edit Form -->
        <div class="form-container">
            <form id="editRestaurantForm" enctype="multipart/form-data">
                <input type="hidden" id="restaurantId" name="id" value="<%= restaurant.id %>">
                <input type="hidden" name="manager_id" value="<%= manager.manager_id || '' %>">
                
                <ul class="nav nav-tabs" id="editTabs">
                    <li class="nav-item">
                        <button class="nav-link active" type="button" data-bs-toggle="tab" data-bs-target="#basicInfoTab">
                            <i class="fas fa-info-circle me-2"></i> Basic Info
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" type="button" data-bs-toggle="tab" data-bs-target="#locationTab">
                            <i class="fas fa-map-marker-alt me-2"></i> Location
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" type="button" data-bs-toggle="tab" data-bs-target="#settingsTab">
                            <i class="fas fa-cog me-2"></i> Settings
                        </button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" type="button" data-bs-toggle="tab" data-bs-target="#imagesTab">
                            <i class="fas fa-images me-2"></i> Images
                        </button>
                    </li>
                </ul>
                
                <div class="tab-content mt-4">
                    <!-- Basic Info Tab -->
                    <div class="tab-pane fade show active" id="basicInfoTab">
                        <div class="row">
                            <div class="col-md-12 mb-3">
                                <label class="form-label">
                                    Restaurant Name
                                    <span class="required">* Required</span>
                                </label>
                                <input type="text" class="form-control" id="name" name="name" required
                                       value="<%= restaurant.name %>">
                                <div class="invalid-feedback">Please enter a restaurant name.</div>
                            </div>
                            
                            <div class="col-md-12 mb-3">
                                <label class="form-label">Description</label>
                                <textarea class="form-control" id="description" name="description" 
                                          rows="4"><%= restaurant.description || '' %></textarea>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Cuisine Types</label>
                                <select class="form-select select2" id="cuisine_type" name="cuisine_type" multiple>
                                    <option value="indian" <%= (restaurant.cuisine_type || '').includes('indian') ? 'selected' : '' %>>Indian</option>
                                    <option value="chinese" <%= (restaurant.cuisine_type || '').includes('chinese') ? 'selected' : '' %>>Chinese</option>
                                    <option value="italian" <%= (restaurant.cuisine_type || '').includes('italian') ? 'selected' : '' %>>Italian</option>
                                    <option value="mexican" <%= (restaurant.cuisine_type || '').includes('mexican') ? 'selected' : '' %>>Mexican</option>
                                    <option value="fast_food" <%= (restaurant.cuisine_type || '').includes('fast_food') ? 'selected' : '' %>>Fast Food</option>
                                    <option value="vegetarian" <%= (restaurant.cuisine_type || '').includes('vegetarian') ? 'selected' : '' %>>Vegetarian</option>
                                    <option value="non_vegetarian" <%= (restaurant.cuisine_type || '').includes('non_vegetarian') ? 'selected' : '' %>>Non-Vegetarian</option>
                                    <option value="south_indian" <%= (restaurant.cuisine_type || '').includes('south_indian') ? 'selected' : '' %>>South Indian</option>
                                    <option value="north_indian" <%= (restaurant.cuisine_type || '').includes('north_indian') ? 'selected' : '' %>>North Indian</option>
                                    <option value="biryani" <%= (restaurant.cuisine_type || '').includes('biryani') ? 'selected' : '' %>>Biryani</option>
                                </select>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">
                                    Phone Number
                                    <span class="required">* Required</span>
                                </label>
                                <input type="tel" class="form-control" id="phone_number" name="phone_number" 
                                       pattern="[0-9]{10}" required value="<%= restaurant.phone_number %>">
                                <div class="invalid-feedback">Please enter a valid 10-digit phone number.</div>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Email Address</label>
                                <input type="email" class="form-control" id="email" name="email" 
                                       value="<%= restaurant.email || '' %>">
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Website</label>
                                <input type="url" class="form-control" id="website" name="website" 
                                       value="<%= restaurant.website || '' %>">
                            </div>
                            
                            <!-- Opening Hours -->
                            <div class="col-md-12 mb-3">
                                <label class="form-label">Opening Hours</label>
                                <div class="hours-container">
                                    <% const hours = restaurant.opening_hours || { weekday: '10:00 AM - 10:00 PM', saturday: '10:00 AM - 11:00 PM', sunday: '10:00 AM - 10:00 PM' }; %>
                                    <div class="day-hours">
                                        <div class="day-label">Monday - Friday</div>
                                        <input type="text" class="form-control" name="weekday_hours" 
                                               value="<%= hours.weekday %>">
                                    </div>
                                    <div class="day-hours">
                                        <div class="day-label">Saturday</div>
                                        <input type="text" class="form-control" name="saturday_hours" 
                                               value="<%= hours.saturday %>">
                                    </div>
                                    <div class="day-hours">
                                        <div class="day-label">Sunday</div>
                                        <input type="text" class="form-control" name="sunday_hours" 
                                               value="<%= hours.sunday %>">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Location Tab -->
                    <div class="tab-pane fade" id="locationTab">
                        <div class="row">
                            <div class="col-md-12 mb-3">
                                <label class="form-label">
                                    Full Address
                                    <span class="required">* Required</span>
                                </label>
                                <textarea class="form-control" id="address" name="address" rows="3" 
                                          required><%= restaurant.address %></textarea>
                                <div class="invalid-feedback">Please enter the restaurant address.</div>
                            </div>
                            
                            <div class="col-md-4 mb-3">
                                <label class="form-label">City</label>
                                <input type="text" class="form-control" id="city" name="city" value="<%= restaurant.city || '' %>">
                            </div>
                            
                            <div class="col-md-4 mb-3">
                                <label class="form-label">State</label>
                                <input type="text" class="form-control" id="state" name="state" value="<%= restaurant.state || '' %>">
                            </div>
                            
                            <div class="col-md-4 mb-3">
                                <label class="form-label">ZIP/PIN Code</label>
                                <input type="text" class="form-control" id="zip_code" name="zip_code" 
                                       pattern="[0-9]{6}" value="<%= restaurant.zip_code || '' %>">
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Country</label>
                                <input type="text" class="form-control" id="country" name="country" value="<%= restaurant.country || 'India' %>">
                            </div>
                            
                            <div class="col-md-3 mb-3">
                                <label class="form-label">Latitude</label>
                                <div class="input-group">
                                    <input type="number" step="any" class="form-control" id="latitude" name="latitude" 
                                           value="<%= restaurant.latitude || '' %>">
                                    <button type="button" class="btn btn-outline-secondary" onclick="getCurrentLocation()">
                                        <i class="fas fa-location-crosshairs"></i>
                                    </button>
                                </div>
                            </div>
                            
                            <div class="col-md-3 mb-3">
                                <label class="form-label">Longitude</label>
                                <div class="input-group">
                                    <input type="number" step="any" class="form-control" id="longitude" name="longitude" 
                                           value="<%= restaurant.longitude || '' %>">
                                    <button type="button" class="btn btn-outline-secondary" onclick="getCurrentLocation()">
                                        <i class="fas fa-location-crosshairs"></i>
                                    </button>
                                </div>
                            </div>
                            
                            <div class="col-md-12">
                                <label class="form-label">Map Preview</label>
                                <div class="map-preview" id="mapPreview">
                                    <div style="height: 100%; display: flex; align-items: center; justify-content: center;">
                                        <p class="text-muted mb-0">
                                            <i class="fas fa-map-marker-alt fa-2x"></i><br>
                                            Map will appear here
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Settings Tab -->
                    <div class="tab-pane fade" id="settingsTab">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="card h-100">
                                    <div class="card-header bg-light">
                                        <h6 class="mb-0"><i class="fas fa-business-time me-2"></i> Operational Settings</h6>
                                    </div>
                                    <div class="card-body">
                                        <div class="mb-3">
                                            <label class="form-label">Preparation Time (minutes)</label>
                                            <input type="number" class="form-control" id="preparation_time_minutes" 
                                                   name="preparation_time_minutes" value="<%= restaurant.preparation_time_minutes || 20 %>" min="5" max="120">
                                        </div>
                                        
                                        <div class="mb-3">
                                            <label class="form-label">Minimum Order Amount (₹)</label>
                                            <input type="number" class="form-control" id="min_order_amount" 
                                                   name="min_order_amount" value="<%= restaurant.min_order_amount || 0 %>" min="0">
                                        </div>
                                        
                                        <div class="mb-3">
                                            <label class="form-label">Delivery Radius (km)</label>
                                            <input type="number" class="form-control" id="delivery_radius" 
                                                   name="delivery_radius" value="<%= restaurant.delivery_radius || 5 %>" min="1" max="20" step="0.5">
                                        </div>
                                        
                                        <div class="mb-3">
                                            <label class="form-label">Packing Charges (₹)</label>
                                            <input type="number" class="form-control" id="packing_charges" 
                                                   name="packing_charges" value="<%= restaurant.packing_charges || 0 %>" min="0">
                                        </div>
                                        
                                        <div class="mb-3">
                                            <label class="form-label">Tax Percentage (%)</label>
                                            <input type="number" class="form-control" id="tax_percentage" 
                                                   name="tax_percentage" value="<%= restaurant.tax_percentage || 5 %>" min="0" max="50" step="0.1">
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-md-6">
                                <div class="card h-100">
                                    <div class="card-header bg-light">
                                        <h6 class="mb-0"><i class="fas fa-toggle-on me-2"></i> Status Settings</h6>
                                    </div>
                                    <div class="card-body">
                                        <div class="form-check form-switch mb-3">
                                            <input class="form-check-input" type="checkbox" id="is_active" name="is_active" 
                                                   <%= restaurant.is_active ? 'checked' : '' %>>
                                            <label class="form-check-label" for="is_active">
                                                Restaurant is Active
                                            </label>
                                        </div>
                                        
                                        <div class="form-check form-switch mb-3">
                                            <input class="form-check-input" type="checkbox" id="is_accepting_orders" name="is_accepting_orders" 
                                                   <%= restaurant.is_accepting_orders ? 'checked' : '' %>>
                                            <label class="form-check-label" for="is_accepting_orders">
                                                Accepting Orders
                                            </label>
                                        </div>
                                        
                                        <div class="form-check form-switch mb-3">
                                            <input class="form-check-input" type="checkbox" id="is_verified" name="is_verified" 
                                                   <%= restaurant.is_verified ? 'checked' : '' %>>
                                            <label class="form-check-label" for="is_verified">
                                                Restaurant is Verified
                                            </label>
                                        </div>
                                        
                                        <div class="form-check form-switch mb-3">
                                            <input class="form-check-input" type="checkbox" id="is_featured" name="is_featured" 
                                                   <%= restaurant.is_featured ? 'checked' : '' %>>
                                            <label class="form-check-label" for="is_featured">
                                                Featured Restaurant
                                            </label>
                                        </div>
                                        
                                        <div class="form-check form-switch mb-3">
                                            <input class="form-check-input" type="checkbox" id="allow_preorder" name="allow_preorder" 
                                                   <%= restaurant.allow_preorder ? 'checked' : '' %>>
                                            <label class="form-check-label" for="allow_preorder">
                                                Allow Pre-orders
                                            </label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Images Tab -->
                    <div class="tab-pane fade" id="imagesTab">
                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label class="form-label">Logo Image</label>
                                <div class="image-upload-container" id="logoUpload" onclick="document.getElementById('logoInput').click()">
                                    <div class="image-actions">
                                        <button type="button" class="image-action-btn" onclick="event.stopPropagation(); removeLogo()">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                    <img id="logoPreview" class="image-preview" 
                                         src="<%= restaurant.logo_url || '' %>" 
                                         <%= restaurant.logo_url ? 'style="display: block;"' : '' %> 
                                         alt="Logo Preview">
                                    <input type="hidden" id="currentLogoUrl" name="current_logo_url" value="<%= restaurant.logo_url || '' %>">
                                    <input type="file" class="d-none" id="logoInput" name="logo" accept="image/*">
                                    <% if (!restaurant.logo_url) { %>
                                        <i class="fas fa-image fa-3x text-muted mb-3"></i>
                                        <p class="mb-2">Click to upload logo</p>
                                        <p class="text-muted small mb-0">Recommended: 500x500px, JPG/PNG</p>
                                    <% } %>
                                </div>
                            </div>
                            
                            <div class="col-md-6 mb-4">
                                <label class="form-label">Cover Image</label>
                                <div class="image-upload-container" id="coverUpload" onclick="document.getElementById('coverInput').click()">
                                    <div class="image-actions">
                                        <button type="button" class="image-action-btn" onclick="event.stopPropagation(); removeCover()">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                    <img id="coverPreview" class="image-preview" 
                                         src="<%= restaurant.cover_image_url || '' %>" 
                                         <%= restaurant.cover_image_url ? 'style="display: block;"' : '' %> 
                                         alt="Cover Preview">
                                    <input type="hidden" id="currentCoverUrl" name="current_cover_url" value="<%= restaurant.cover_image_url || '' %>">
                                    <input type="file" class="d-none" id="coverInput" name="cover_image" accept="image/*">
                                    <% if (!restaurant.cover_image_url) { %>
                                        <i class="fas fa-image fa-3x text-muted mb-3"></i>
                                        <p class="mb-2">Click to upload cover</p>
                                        <p class="text-muted small mb-0">Recommended: 1200x400px, JPG/PNG</p>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="mt-4 pt-4 border-top">
                    <div class="d-flex justify-content-between">
                        <button type="button" class="btn btn-secondary" onclick="window.history.back()">
                            <i class="fas fa-times me-1"></i> Cancel
                        </button>
                        <button type="button" class="btn btn-primary" onclick="saveChanges()">
                            <i class="fas fa-save me-1"></i> Save Changes
                        </button>
                    </div>
                </div>
            </form>
        </div>
        
        <!-- Toast Notification -->
        <div class="toast-container">
            <div class="toast" id="notificationToast" role="alert">
                <div class="toast-header">
                    <strong class="me-auto" id="toastTitle">Notification</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
                </div>
                <div class="toast-body" id="toastMessage"></div>
            </div>
        </div>
        
        <!-- Delete Confirmation Modal -->
        <div class="modal fade" id="deleteModal" tabindex="-1">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header bg-danger text-white">
                        <h5 class="modal-title"><i class="fas fa-exclamation-triangle me-2"></i> Confirm Delete</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <p>Are you sure you want to delete <strong><%= restaurant.name %></strong>?</p>
                        <p class="text-danger">
                            <i class="fas fa-exclamation-triangle me-1"></i> 
                            This action cannot be undone. All menu items, orders, and restaurant data will be permanently deleted.
                        </p>
                        <p>Please type the restaurant name to confirm:</p>
                        <input type="text" class="form-control" id="confirmDeleteInput" placeholder="Type: <%= restaurant.name %>">
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-danger" id="confirmDeleteBtn" disabled onclick="deleteRestaurant()">
                            <i class="fas fa-trash me-1"></i> Delete Restaurant
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script>
        // Global variables
        const restaurantId = '<%= restaurant.id %>';
        const API_BASE = '/api/restaurants';
        
        // Toast notification
        const toast = new bootstrap.Toast(document.getElementById('notificationToast'));
        
        function showNotification(title, message, type = 'info') {
            const toastTitle = document.getElementById('toastTitle');
            const toastMessage = document.getElementById('toastMessage');
            
            toastTitle.textContent = title;
            toastMessage.textContent = message;
            
            const toastHeader = document.querySelector('#notificationToast .toast-header');
            const types = {
                'success': 'bg-success text-white',
                'error': 'bg-danger text-white',
                'warning': 'bg-warning text-dark',
                'info': 'bg-info text-white'
            };
            
            toastHeader.className = `toast-header ${types[type] || 'bg-info text-white'}`;
            toast.show();
        }
        
        // Loading spinner
        function showLoading() {
            document.getElementById('loadingSpinner').style.display = 'flex';
        }
        
        function hideLoading() {
            document.getElementById('loadingSpinner').style.display = 'none';
        }
        
        // Initialize Select2
        $(document).ready(function() {
            $('.select2').select2({
                placeholder: 'Select cuisine types',
                allowClear: true,
                width: '100%'
            });
            
            // Initialize map preview
            updateMapPreview();
            
            // Setup event listeners
            setupEventListeners();
        });
        
        function setupEventListeners() {
            // Image upload previews
            document.getElementById('logoInput').addEventListener('change', function(e) {
                if (validateImage(e.target.files[0], 2)) {
                    previewImage(e.target.files[0], 'logoPreview');
                }
            });
            
            document.getElementById('coverInput').addEventListener('change', function(e) {
                if (validateImage(e.target.files[0], 5)) {
                    previewImage(e.target.files[0], 'coverPreview');
                }
            });
            
            // Location coordinates changes
            document.getElementById('latitude').addEventListener('input', updateMapPreview);
            document.getElementById('longitude').addEventListener('input', updateMapPreview);
            
            // Delete confirmation input
            document.getElementById('confirmDeleteInput').addEventListener('input', function() {
                const confirmBtn = document.getElementById('confirmDeleteBtn');
                confirmBtn.disabled = this.value !== '<%= restaurant.name %>';
            });
            
            // Drag and drop for image uploads
            ['logoUpload', 'coverUpload'].forEach(id => {
                const container = document.getElementById(id);
                container.addEventListener('dragover', function(e) {
                    e.preventDefault();
                    this.classList.add('dragover');
                });
                
                container.addEventListener('dragleave', function(e) {
                    e.preventDefault();
                    this.classList.remove('dragover');
                });
                
                container.addEventListener('drop', function(e) {
                    e.preventDefault();
                    this.classList.remove('dragover');
                    
                    const files = e.dataTransfer.files;
                    if (files.length > 0) {
                        const inputId = id === 'logoUpload' ? 'logoInput' : 'coverInput';
                        document.getElementById(inputId).files = files;
                        const event = new Event('change');
                        document.getElementById(inputId).dispatchEvent(event);
                    }
                });
            });
        }
        
        function validateImage(file, maxSizeMB) {
            const maxSize = maxSizeMB * 1024 * 1024;
            const allowedTypes = ['image/jpeg', 'image/png', 'image/jpg', 'image/gif'];
            
            if (!allowedTypes.includes(file.type)) {
                showNotification('Error', 'Please upload only JPG, PNG, or GIF images', 'error');
                return false;
            }
            
            if (file.size > maxSize) {
                showNotification('Error', `Image size should be less than ${maxSizeMB}MB`, 'error');
                return false;
            }
            
            return true;
        }
        
        function previewImage(file, previewId) {
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const preview = document.getElementById(previewId);
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        }
        
        function removeLogo() {
            if (confirm('Are you sure you want to remove the logo?')) {
                document.getElementById('logoPreview').style.display = 'none';
                document.getElementById('logoPreview').src = '';
                document.getElementById('currentLogoUrl').value = '';
                document.getElementById('logoInput').value = '';
                
                // Add upload prompt back
                const container = document.getElementById('logoUpload');
                const uploadPrompt = document.createElement('div');
                uploadPrompt.innerHTML = `
                    <i class="fas fa-image fa-3x text-muted mb-3"></i>
                    <p class="mb-2">Click to upload logo</p>
                    <p class="text-muted small mb-0">Recommended: 500x500px, JPG/PNG</p>
                `;
                container.appendChild(uploadPrompt);
            }
        }
        
        function removeCover() {
            if (confirm('Are you sure you want to remove the cover image?')) {
                document.getElementById('coverPreview').style.display = 'none';
                document.getElementById('coverPreview').src = '';
                document.getElementById('currentCoverUrl').value = '';
                document.getElementById('coverInput').value = '';
                
                // Add upload prompt back
                const container = document.getElementById('coverUpload');
                const uploadPrompt = document.createElement('div');
                uploadPrompt.innerHTML = `
                    <i class="fas fa-image fa-3x text-muted mb-3"></i>
                    <p class="mb-2">Click to upload cover</p>
                    <p class="text-muted small mb-0">Recommended: 1200x400px, JPG/PNG</p>
                `;
                container.appendChild(uploadPrompt);
            }
        }
        
        // Get current location
        function getCurrentLocation() {
            if (navigator.geolocation) {
                showNotification('Info', 'Getting your current location...', 'info');
                
                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        document.getElementById('latitude').value = position.coords.latitude.toFixed(6);
                        document.getElementById('longitude').value = position.coords.longitude.toFixed(6);
                        showNotification('Success', 'Location fetched successfully!', 'success');
                        updateMapPreview();
                    },
                    (error) => {
                        console.error('Geolocation error:', error);
                        showNotification('Error', 'Failed to get location: ' + error.message, 'error');
                    }
                );
            } else {
                showNotification('Error', 'Geolocation is not supported by your browser', 'error');
            }
        }
        
        function updateMapPreview() {
            const lat = document.getElementById('latitude').value;
            const lng = document.getElementById('longitude').value;
            
            if (lat && lng) {
                const mapPreview = document.getElementById('mapPreview');
                mapPreview.innerHTML = `
                    <iframe 
                        width="100%" 
                        height="100%" 
                        frameborder="0" 
                        scrolling="no" 
                        marginheight="0" 
                        marginwidth="0" 
                        src="https://maps.google.com/maps?q=${lat},${lng}&z=15&output=embed"
                        style="border: none;">
                    </iframe>
                `;
            }
        }
        
        // Save changes
        async function saveChanges() {
            const form = document.getElementById('editRestaurantForm');
            
            // Validate form
            if (!form.checkValidity()) {
                form.classList.add('was-validated');
                showNotification('Error', 'Please fill in all required fields correctly', 'error');
                return;
            }
            
            try {
                showLoading();
                
                // Prepare form data
                const formData = new FormData(form);
                
                // Convert cuisine array to string
                const cuisines = $('#cuisine_type').val();
                if (cuisines) {
                    formData.set('cuisine_type', cuisines.join(','));
                }
                
                // Prepare opening hours object
                const openingHours = {
                    weekday: formData.get('weekday_hours'),
                    saturday: formData.get('saturday_hours'),
                    sunday: formData.get('sunday_hours'),
                    display: `${formData.get('weekday_hours')} (Mon-Fri), ${formData.get('saturday_hours')} (Sat), ${formData.get('sunday_hours')} (Sun)`
                };
                formData.set('opening_hours', JSON.stringify(openingHours));
                
                // Remove temporary fields
                ['weekday_hours', 'saturday_hours', 'sunday_hours'].forEach(field => formData.delete(field));
                
                // Convert booleans
                ['is_active', 'is_accepting_orders', 'is_verified', 'is_featured', 'allow_preorder'].forEach(field => {
                    formData.set(field, formData.get(field) === 'on' ? 'true' : 'false');
                });
                
                // Send update request
                const response = await fetch(`${API_BASE}/${restaurantId}`, {
                    method: 'PUT',
                    headers: {
                        'Authorization': `Bearer ${getToken()}`
                    },
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showNotification('Success', 'Restaurant updated successfully!', 'success');
                    
                    // Update display values
                    document.getElementById('currentName').textContent = document.getElementById('name').value;
                    document.getElementById('currentDescription').textContent = document.getElementById('description').value;
                    document.getElementById('currentAddress').textContent = document.getElementById('address').value;
                    document.getElementById('currentPhone').textContent = document.getElementById('phone_number').value;
                    
                    // Update logo if changed
                    const newLogo = document.getElementById('logoPreview').src;
                    if (newLogo && newLogo.startsWith('data:')) {
                        document.getElementById('currentLogo').src = newLogo;
                    }
                    
                    // Reload page after delay to reflect all changes
                    setTimeout(() => {
                        window.location.reload();
                    }, 2000);
                } else {
                    showNotification('Error', data.error || 'Failed to update restaurant', 'error');
                }
            } catch (error) {
                console.error('Error updating restaurant:', error);
                showNotification('Error', 'Failed to update restaurant: ' + error.message, 'error');
            } finally {
                hideLoading();
            }
        }
        
        // Delete restaurant
        function showDeleteConfirmation() {
            const modal = new bootstrap.Modal(document.getElementById('deleteModal'));
            modal.show();
        }
        
        async function deleteRestaurant() {
            try {
                showLoading();
                
                // Close modal
                const modal = bootstrap.Modal.getInstance(document.getElementById('deleteModal'));
                modal.hide();
                
                // Send delete request
                const response = await fetch(`${API_BASE}/${restaurantId}`, {
                    method: 'DELETE',
                    headers: {
                        'Authorization': `Bearer ${getToken()}`
                    }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showNotification('Success', 'Restaurant deleted successfully', 'success');
                    
                    // Redirect to restaurants page
                    setTimeout(() => {
                        window.location.href = '/manager/restaurants';
                    }, 1500);
                } else {
                    showNotification('Error', data.error || 'Failed to delete restaurant', 'error');
                }
            } catch (error) {
                console.error('Error deleting restaurant:', error);
                showNotification('Error', 'Failed to delete restaurant', 'error');
            } finally {
                hideLoading();
            }
        }
        
        // Get JWT token
        function getToken() {
            return localStorage.getItem('manager_token') || '';
        }
    </script>
</body>
</html>