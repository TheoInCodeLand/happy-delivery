<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Restaurant - Restaurant Manager</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <style>
        :root {
            --primary-color: #4a6fa5;
            --secondary-color: #166088;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --info-color: #17a2b8;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
        }
        
        /* Header */
        .page-header {
            background: white;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            margin-bottom: 30px;
            border-left: 5px solid var(--primary-color);
        }
        
        .page-header h2 {
            margin: 0;
            color: var(--primary-color);
            font-weight: 700;
        }
        
        .page-header p {
            margin: 5px 0 0 0;
            color: #666;
            font-size: 0.95rem;
        }
        
        /* Form Container */
        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            padding: 30px;
            margin-bottom: 30px;
        }
        
        /* Stepper */
        .stepper {
            display: flex;
            justify-content: space-between;
            margin-bottom: 40px;
            position: relative;
        }
        
        .stepper::before {
            content: '';
            position: absolute;
            top: 20px;
            left: 0;
            right: 0;
            height: 2px;
            background: #e0e0e0;
            z-index: 1;
        }
        
        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 2;
            flex: 1;
        }
        
        .step-number {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: white;
            border: 2px solid #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: #666;
            margin-bottom: 10px;
            transition: all 0.3s;
        }
        
        .step.active .step-number {
            background: var(--primary-color);
            border-color: var(--primary-color);
            color: white;
            transform: scale(1.1);
        }
        
        .step.completed .step-number {
            background: var(--success-color);
            border-color: var(--success-color);
            color: white;
        }
        
        .step-label {
            font-size: 0.9rem;
            color: #666;
            font-weight: 500;
            text-align: center;
        }
        
        .step.active .step-label {
            color: var(--primary-color);
            font-weight: 600;
        }
        
        /* Form Steps */
        .form-step {
            display: none;
            animation: fadeIn 0.5s ease;
        }
        
        .form-step.active {
            display: block;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Form Elements */
        .form-label {
            font-weight: 600;
            color: #555;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .form-label .required {
            color: var(--danger-color);
            font-size: 0.8rem;
        }
        
        .form-control, .form-select {
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 12px 15px;
            transition: all 0.3s;
            font-size: 0.95rem;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(74, 111, 165, 0.15);
        }
        
        .form-control.is-invalid {
            border-color: var(--danger-color);
        }
        
        .invalid-feedback {
            color: var(--danger-color);
            font-size: 0.85rem;
            margin-top: 5px;
        }
        
        /* Image Upload */
        .image-upload-container {
            border: 2px dashed #ddd;
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
            background-color: #f8f9fa;
        }
        
        .image-upload-container:hover {
            border-color: var(--primary-color);
            background-color: rgba(74, 111, 165, 0.05);
        }
        
        .image-upload-container.dragover {
            border-color: var(--success-color);
            background-color: rgba(40, 167, 69, 0.05);
        }
        
        .image-preview {
            max-width: 200px;
            max-height: 200px;
            border-radius: 10px;
            object-fit: cover;
            margin: 15px auto;
            display: none;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }
        
        /* Opening Hours */
        .hours-container {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
        }
        
        .day-hours {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
            padding: 10px;
            background: white;
            border-radius: 8px;
        }
        
        .day-label {
            min-width: 120px;
            font-weight: 600;
            color: #555;
        }
        
        /* Toggle Switches */
        .form-check-input {
            width: 2.5em;
            height: 1.25em;
        }
        
        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        /* Button Groups */
        .btn-group {
            display: flex;
            justify-content: space-between;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #eee;
        }
        
        .btn {
            padding: 12px 30px;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            border: none;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(74, 111, 165, 0.3);
        }
        
        .btn-secondary {
            border: 2px solid #ddd;
        }
        
        /* Loading Spinner */
        .spinner-container {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255,255,255,0.9);
            z-index: 9999;
            justify-content: center;
            align-items: center;
        }
        
        .spinner {
            width: 50px;
            height: 50px;
            border: 5px solid #f3f3f3;
            border-top: 5px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* Toast Notification */
        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }
        
        .toast {
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.15);
            border: none;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
            
            .stepper {
                flex-direction: column;
                align-items: flex-start;
                gap: 20px;
            }
            
            .stepper::before {
                display: none;
            }
            
            .step {
                flex-direction: row;
                gap: 15px;
            }
            
            .btn-group {
                flex-direction: column;
                gap: 10px;
            }
            
            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="spinner-container" id="loadingSpinner">
        <div class="spinner"></div>
    </div>
    
    <div class="container py-4">
        <!-- Header -->
        <div class="page-header">
            <div class="d-flex justify-content-between align-items-start">
                <div>
                    <h2><i class="fas fa-plus-circle me-2"></i> Add New Restaurant</h2>
                    <p>Fill in all the details to add your restaurant to the platform</p>
                </div>
                <div>
                    <a href="/manager/restaurants" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i> Back to Restaurants
                    </a>
                </div>
            </div>
        </div>
        
        <!-- Stepper -->
        <div class="stepper">
            <div class="step active" data-step="1">
                <div class="step-number">1</div>
                <div class="step-label">Basic Info</div>
            </div>
            <div class="step" data-step="2">
                <div class="step-number">2</div>
                <div class="step-label">Location</div>
            </div>
            <div class="step" data-step="3">
                <div class="step-number">3</div>
                <div class="step-label">Settings</div>
            </div>
            <div class="step" data-step="4">
                <div class="step-number">4</div>
                <div class="step-label">Images</div>
            </div>
        </div>
        
        <!-- Form Container -->
        <div class="form-container">
            <form id="addRestaurantForm" enctype="multipart/form-data">
                <input type="hidden" name="manager_id" value="<%= manager.manager_id || '' %>">
                
                <!-- Step 1: Basic Info -->
                <div class="form-step active" id="step-1">
                    <h4 class="mb-4"><i class="fas fa-info-circle me-2 text-primary"></i> Basic Information</h4>
                    
                    <div class="row">
                        <div class="col-md-12 mb-3">
                            <label class="form-label">
                                Restaurant Name
                                <span class="required">* Required</span>
                            </label>
                            <input type="text" class="form-control" id="name" name="name" required
                                   placeholder="e.g., Mumbai Kitchen">
                            <div class="invalid-feedback">Please enter a restaurant name.</div>
                        </div>
                        
                        <div class="col-md-12 mb-3">
                            <label class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" 
                                      rows="4" placeholder="Tell customers about your restaurant..."></textarea>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Cuisine Types</label>
                            <select class="form-select select2" id="cuisine_type" name="cuisine_type" multiple>
                                <option value="indian">Indian</option>
                                <option value="chinese">Chinese</option>
                                <option value="italian">Italian</option>
                                <option value="mexican">Mexican</option>
                                <option value="fast_food">Fast Food</option>
                                <option value="vegetarian">Vegetarian</option>
                                <option value="non_vegetarian">Non-Vegetarian</option>
                                <option value="south_indian">South Indian</option>
                                <option value="north_indian">North Indian</option>
                                <option value="biryani">Biryani</option>
                                <option value="thai">Thai</option>
                                <option value="japanese">Japanese</option>
                            </select>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label class="form-label">
                                Phone Number
                                <span class="required">* Required</span>
                            </label>
                            <input type="tel" class="form-control" id="phone_number" name="phone_number" 
                                   pattern="[0-9]{10}" required placeholder="10-digit mobile number">
                            <div class="invalid-feedback">Please enter a valid 10-digit phone number.</div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="email" name="email" 
                                   placeholder="restaurant@example.com">
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Website</label>
                            <input type="url" class="form-control" id="website" name="website" 
                                   placeholder="https://your-restaurant.com">
                        </div>
                        
                        <!-- Opening Hours -->
                        <div class="col-md-12 mb-3">
                            <label class="form-label">Opening Hours</label>
                            <div class="hours-container">
                                <div class="day-hours">
                                    <div class="day-label">Monday - Friday</div>
                                    <input type="text" class="form-control" name="weekday_hours" 
                                           value="10:00 AM - 10:00 PM">
                                </div>
                                <div class="day-hours">
                                    <div class="day-label">Saturday</div>
                                    <input type="text" class="form-control" name="saturday_hours" 
                                           value="10:00 AM - 11:00 PM">
                                </div>
                                <div class="day-hours">
                                    <div class="day-label">Sunday</div>
                                    <input type="text" class="form-control" name="sunday_hours" 
                                           value="10:00 AM - 10:00 PM">
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="btn-group">
                        <div></div>
                        <button type="button" class="btn btn-primary" onclick="nextStep(2)">
                            Next <i class="fas fa-arrow-right ms-1"></i>
                        </button>
                    </div>
                </div>
                
                <!-- Step 2: Location -->
                <div class="form-step" id="step-2">
                    <h4 class="mb-4"><i class="fas fa-map-marker-alt me-2 text-primary"></i> Location Details</h4>
                    
                    <div class="row">
                        <div class="col-md-12 mb-3">
                            <label class="form-label">
                                Full Address
                                <span class="required">* Required</span>
                            </label>
                            <textarea class="form-control" id="address" name="address" rows="3" 
                                      required placeholder="Full address including city, state, and PIN code"></textarea>
                            <div class="invalid-feedback">Please enter the restaurant address.</div>
                        </div>
                        
                        <div class="col-md-4 mb-3">
                            <label class="form-label">City</label>
                            <input type="text" class="form-control" id="city" name="city" placeholder="City">
                        </div>
                        
                        <div class="col-md-4 mb-3">
                            <label class="form-label">State</label>
                            <input type="text" class="form-control" id="state" name="state" placeholder="State">
                        </div>
                        
                        <div class="col-md-4 mb-3">
                            <label class="form-label">ZIP/PIN Code</label>
                            <input type="text" class="form-control" id="zip_code" name="zip_code" 
                                   pattern="[0-9]{6}" placeholder="6-digit PIN code">
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Country</label>
                            <input type="text" class="form-control" id="country" name="country" value="India">
                        </div>
                        
                        <div class="col-md-3 mb-3">
                            <label class="form-label">Latitude</label>
                            <div class="input-group">
                                <input type="number" step="any" class="form-control" id="latitude" name="latitude" 
                                       placeholder="e.g., 19.0760">
                                <button type="button" class="btn btn-outline-secondary" onclick="getCurrentLocation()">
                                    <i class="fas fa-location-crosshairs"></i>
                                </button>
                            </div>
                        </div>
                        
                        <div class="col-md-3 mb-3">
                            <label class="form-label">Longitude</label>
                            <div class="input-group">
                                <input type="number" step="any" class="form-control" id="longitude" name="longitude" 
                                       placeholder="e.g., 72.8777">
                                <button type="button" class="btn btn-outline-secondary" onclick="getCurrentLocation()">
                                    <i class="fas fa-location-crosshairs"></i>
                                </button>
                            </div>
                        </div>
                        
                        <div class="col-md-12">
                            <div class="card">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0"><i class="fas fa-map me-2"></i> Map Preview</h6>
                                </div>
                                <div class="card-body text-center p-0">
                                    <div id="mapPreview" style="height: 200px; background: #f8f9fa; display: flex; align-items: center; justify-content: center;">
                                        <p class="text-muted mb-0">
                                            <i class="fas fa-map-marker-alt fa-2x"></i><br>
                                            Coordinates will appear here
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="btn-group">
                        <button type="button" class="btn btn-secondary" onclick="prevStep(1)">
                            <i class="fas fa-arrow-left me-1"></i> Back
                        </button>
                        <button type="button" class="btn btn-primary" onclick="nextStep(3)">
                            Next <i class="fas fa-arrow-right ms-1"></i>
                        </button>
                    </div>
                </div>
                
                <!-- Step 3: Settings -->
                <div class="form-step" id="step-3">
                    <h4 class="mb-4"><i class="fas fa-cog me-2 text-primary"></i> Business Settings</h4>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="card h-100">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0"><i class="fas fa-business-time me-2"></i> Operational Settings</h6>
                                </div>
                                <div class="card-body">
                                    <div class="mb-3">
                                        <label class="form-label">Preparation Time (minutes)</label>
                                        <input type="number" class="form-control" id="preparation_time_minutes" 
                                               name="preparation_time_minutes" value="20" min="5" max="120">
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Minimum Order Amount (₹)</label>
                                        <input type="number" class="form-control" id="min_order_amount" 
                                               name="min_order_amount" value="0" min="0">
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Delivery Radius (km)</label>
                                        <input type="number" class="form-control" id="delivery_radius" 
                                               name="delivery_radius" value="5" min="1" max="20" step="0.5">
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Packing Charges (₹)</label>
                                        <input type="number" class="form-control" id="packing_charges" 
                                               name="packing_charges" value="0" min="0">
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Tax Percentage (%)</label>
                                        <input type="number" class="form-control" id="tax_percentage" 
                                               name="tax_percentage" value="5" min="0" max="50" step="0.1">
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="card h-100">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0"><i class="fas fa-toggle-on me-2"></i> Status Settings</h6>
                                </div>
                                <div class="card-body">
                                    <div class="form-check form-switch mb-3">
                                        <input class="form-check-input" type="checkbox" id="is_active" name="is_active" checked>
                                        <label class="form-check-label" for="is_active">
                                            Restaurant is Active
                                        </label>
                                        <small class="text-muted d-block">Customers can see and order from active restaurants</small>
                                    </div>
                                    
                                    <div class="form-check form-switch mb-3">
                                        <input class="form-check-input" type="checkbox" id="is_accepting_orders" name="is_accepting_orders" checked>
                                        <label class="form-check-label" for="is_accepting_orders">
                                            Accepting Orders
                                        </label>
                                        <small class="text-muted d-block">Allow customers to place orders</small>
                                    </div>
                                    
                                    <div class="form-check form-switch mb-3">
                                        <input class="form-check-input" type="checkbox" id="is_verified" name="is_verified">
                                        <label class="form-check-label" for="is_verified">
                                            Restaurant is Verified
                                        </label>
                                        <small class="text-muted d-block">Show verified badge on restaurant profile</small>
                                    </div>
                                    
                                    <div class="form-check form-switch mb-3">
                                        <input class="form-check-input" type="checkbox" id="is_featured" name="is_featured">
                                        <label class="form-check-label" for="is_featured">
                                            Featured Restaurant
                                        </label>
                                        <small class="text-muted d-block">Show restaurant in featured section</small>
                                    </div>
                                    
                                    <div class="form-check form-switch mb-3">
                                        <input class="form-check-input" type="checkbox" id="allow_preorder" name="allow_preorder">
                                        <label class="form-check-label" for="allow_preorder">
                                            Allow Pre-orders
                                        </label>
                                        <small class="text-muted d-block">Allow customers to schedule orders for future</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="btn-group">
                        <button type="button" class="btn btn-secondary" onclick="prevStep(2)">
                            <i class="fas fa-arrow-left me-1"></i> Back
                        </button>
                        <button type="button" class="btn btn-primary" onclick="nextStep(4)">
                            Next <i class="fas fa-arrow-right ms-1"></i>
                        </button>
                    </div>
                </div>
                
                <!-- Step 4: Images -->
                <div class="form-step" id="step-4">
                    <h4 class="mb-4"><i class="fas fa-images me-2 text-primary"></i> Restaurant Images</h4>
                    
                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label">Logo Image</label>
                            <div class="image-upload-container" id="logoUpload" onclick="document.getElementById('logoInput').click()">
                                <i class="fas fa-image fa-3x text-muted mb-3"></i>
                                <p class="mb-2">Click to upload logo</p>
                                <p class="text-muted small mb-0">Recommended: 500x500px, JPG/PNG</p>
                                <p class="text-muted small">Max size: 2MB</p>
                                <input type="file" class="d-none" id="logoInput" name="logo" accept="image/*">
                                <img id="logoPreview" class="image-preview" src="" alt="Logo Preview">
                            </div>
                        </div>
                        
                        <div class="col-md-6 mb-4">
                            <label class="form-label">Cover Image</label>
                            <div class="image-upload-container" id="coverUpload" onclick="document.getElementById('coverInput').click()">
                                <i class="fas fa-image fa-3x text-muted mb-3"></i>
                                <p class="mb-2">Click to upload cover</p>
                                <p class="text-muted small mb-0">Recommended: 1200x400px, JPG/PNG</p>
                                <p class="text-muted small">Max size: 5MB</p>
                                <input type="file" class="d-none" id="coverInput" name="cover_image" accept="image/*">
                                <img id="coverPreview" class="image-preview" src="" alt="Cover Preview">
                            </div>
                        </div>
                        
                        <div class="col-md-12">
                            <label class="form-label">Gallery Images</label>
                            <div class="image-upload-container" id="galleryUpload" onclick="document.getElementById('galleryInput').click()">
                                <i class="fas fa-images fa-3x text-muted mb-3"></i>
                                <p class="mb-2">Click to upload gallery images</p>
                                <p class="text-muted small mb-0">You can upload multiple images at once</p>
                                <p class="text-muted small">Recommended: 800x600px, JPG/PNG, Max 5MB each</p>
                                <input type="file" class="d-none" id="galleryInput" name="gallery_images" accept="image/*" multiple>
                                <div id="galleryPreviews" class="d-flex flex-wrap gap-2 mt-3 justify-content-center"></div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="btn-group">
                        <button type="button" class="btn btn-secondary" onclick="prevStep(3)">
                            <i class="fas fa-arrow-left me-1"></i> Back
                        </button>
                        <button type="button" class="btn btn-success" onclick="submitForm()">
                            <i class="fas fa-check me-1"></i> Save Restaurant
                        </button>
                    </div>
                </div>
            </form>
        </div>
        
        <!-- Toast Notification -->
        <div class="toast-container">
            <div class="toast" id="notificationToast" role="alert">
                <div class="toast-header">
                    <strong class="me-auto" id="toastTitle">Notification</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
                </div>
                <div class="toast-body" id="toastMessage"></div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script>
        // Global variables
        let currentStep = 1;
        const API_BASE = '/api/restaurants';
        
        // Toast notification
        const toast = new bootstrap.Toast(document.getElementById('notificationToast'));
        
        function showNotification(title, message, type = 'info') {
            const toastTitle = document.getElementById('toastTitle');
            const toastMessage = document.getElementById('toastMessage');
            
            toastTitle.textContent = title;
            toastMessage.textContent = message;
            
            const toastHeader = document.querySelector('#notificationToast .toast-header');
            const types = {
                'success': 'bg-success text-white',
                'error': 'bg-danger text-white',
                'warning': 'bg-warning text-dark',
                'info': 'bg-info text-white'
            };
            
            toastHeader.className = `toast-header ${types[type] || 'bg-info text-white'}`;
            toast.show();
        }
        
        // Loading spinner
        function showLoading() {
            document.getElementById('loadingSpinner').style.display = 'flex';
        }
        
        function hideLoading() {
            document.getElementById('loadingSpinner').style.display = 'none';
        }
        
        // Initialize Select2
        $(document).ready(function() {
            $('.select2').select2({
                placeholder: 'Select cuisine types',
                allowClear: true,
                width: '100%'
            });
            
            // Setup image uploads
            setupImageUploads();
            
            // Listen for coordinates changes
            document.getElementById('latitude').addEventListener('input', updateMapPreview);
            document.getElementById('longitude').addEventListener('input', updateMapPreview);
        });
        
        function setupImageUploads() {
            // Logo upload
            document.getElementById('logoInput').addEventListener('change', function(e) {
                if (validateImage(e.target.files[0], 2)) {
                    previewImage(e.target.files[0], 'logoPreview');
                }
            });
            
            // Cover upload
            document.getElementById('coverInput').addEventListener('change', function(e) {
                if (validateImage(e.target.files[0], 5)) {
                    previewImage(e.target.files[0], 'coverPreview');
                }
            });
            
            // Gallery upload
            document.getElementById('galleryInput').addEventListener('change', function(e) {
                const galleryPreviews = document.getElementById('galleryPreviews');
                galleryPreviews.innerHTML = '';
                
                Array.from(e.target.files).forEach(file => {
                    if (validateImage(file, 5)) {
                        const reader = new FileReader();
                        reader.onload = function(e) {
                            const img = document.createElement('img');
                            img.src = e.target.result;
                            img.className = 'img-thumbnail';
                            img.style.width = '100px';
                            img.style.height = '100px';
                            img.style.objectFit = 'cover';
                            galleryPreviews.appendChild(img);
                        };
                        reader.readAsDataURL(file);
                    }
                });
            });
            
            // Drag and drop
            ['logoUpload', 'coverUpload', 'galleryUpload'].forEach(id => {
                const container = document.getElementById(id);
                container.addEventListener('dragover', function(e) {
                    e.preventDefault();
                    this.classList.add('dragover');
                });
                
                container.addEventListener('dragleave', function(e) {
                    e.preventDefault();
                    this.classList.remove('dragover');
                });
                
                container.addEventListener('drop', function(e) {
                    e.preventDefault();
                    this.classList.remove('dragover');
                    
                    const files = e.dataTransfer.files;
                    if (files.length > 0) {
                        if (id === 'galleryUpload') {
                            document.getElementById('galleryInput').files = files;
                            const event = new Event('change');
                            document.getElementById('galleryInput').dispatchEvent(event);
                        } else {
                            const inputId = id === 'logoUpload' ? 'logoInput' : 'coverInput';
                            document.getElementById(inputId).files = files;
                            const event = new Event('change');
                            document.getElementById(inputId).dispatchEvent(event);
                        }
                    }
                });
            });
        }
        
        function validateImage(file, maxSizeMB) {
            const maxSize = maxSizeMB * 1024 * 1024;
            const allowedTypes = ['image/jpeg', 'image/png', 'image/jpg', 'image/gif'];
            
            if (!allowedTypes.includes(file.type)) {
                showNotification('Error', 'Please upload only JPG, PNG, or GIF images', 'error');
                return false;
            }
            
            if (file.size > maxSize) {
                showNotification('Error', `Image size should be less than ${maxSizeMB}MB`, 'error');
                return false;
            }
            
            return true;
        }
        
        function previewImage(file, previewId) {
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const preview = document.getElementById(previewId);
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        }
        
        // Stepper navigation
        function nextStep(step) {
            // Validate current step
            if (!validateStep(currentStep)) {
                showNotification('Error', 'Please fill in all required fields', 'error');
                return;
            }
            
            // Hide current step
            document.getElementById(`step-${currentStep}`).classList.remove('active');
            document.querySelector(`.step[data-step="${currentStep}"]`).classList.remove('active');
            
            // Show next step
            currentStep = step;
            document.getElementById(`step-${currentStep}`).classList.add('active');
            document.querySelector(`.step[data-step="${currentStep}"]`).classList.add('active');
            
            // Mark previous step as completed
            for (let i = 1; i < currentStep; i++) {
                document.querySelector(`.step[data-step="${i}"]`).classList.add('completed');
            }
            
            // Update map preview if on location step
            if (currentStep === 2) {
                updateMapPreview();
            }
        }
        
        function prevStep(step) {
            // Hide current step
            document.getElementById(`step-${currentStep}`).classList.remove('active');
            document.querySelector(`.step[data-step="${currentStep}"]`).classList.remove('active');
            
            // Show previous step
            currentStep = step;
            document.getElementById(`step-${currentStep}`).classList.add('active');
            document.querySelector(`.step[data-step="${currentStep}"]`).classList.add('active');
        }
        
        function validateStep(step) {
            let isValid = true;
            
            switch(step) {
                case 1:
                    const name = document.getElementById('name');
                    const phone = document.getElementById('phone_number');
                    
                    if (!name.value.trim()) {
                        name.classList.add('is-invalid');
                        isValid = false;
                    } else {
                        name.classList.remove('is-invalid');
                    }
                    
                    if (!phone.value.trim() || !/^\d{10}$/.test(phone.value)) {
                        phone.classList.add('is-invalid');
                        isValid = false;
                    } else {
                        phone.classList.remove('is-invalid');
                    }
                    break;
                    
                case 2:
                    const address = document.getElementById('address');
                    if (!address.value.trim()) {
                        address.classList.add('is-invalid');
                        isValid = false;
                    } else {
                        address.classList.remove('is-invalid');
                    }
                    break;
                    
                case 3:
                    // No validation needed for settings
                    break;
                    
                case 4:
                    // Image validation is optional
                    break;
            }
            
            return isValid;
        }
        
        // Get current location
        function getCurrentLocation() {
            if (navigator.geolocation) {
                showNotification('Info', 'Getting your current location...', 'info');
                
                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        document.getElementById('latitude').value = position.coords.latitude.toFixed(6);
                        document.getElementById('longitude').value = position.coords.longitude.toFixed(6);
                        showNotification('Success', 'Location fetched successfully!', 'success');
                        updateMapPreview();
                    },
                    (error) => {
                        console.error('Geolocation error:', error);
                        showNotification('Error', 'Failed to get location: ' + error.message, 'error');
                    }
                );
            } else {
                showNotification('Error', 'Geolocation is not supported by your browser', 'error');
            }
        }
        
        function updateMapPreview() {
            const lat = document.getElementById('latitude').value;
            const lng = document.getElementById('longitude').value;
            
            if (lat && lng) {
                const mapPreview = document.getElementById('mapPreview');
                mapPreview.innerHTML = `
                    <iframe 
                        width="100%" 
                        height="100%" 
                        frameborder="0" 
                        scrolling="no" 
                        marginheight="0" 
                        marginwidth="0" 
                        src="https://maps.google.com/maps?q=${lat},${lng}&z=15&output=embed"
                        style="border: none;">
                    </iframe>
                `;
            }
        }
        
        // Submit form
        async function submitForm() {
            const form = document.getElementById('addRestaurantForm');
            
            // Validate all steps
            for (let i = 1; i <= 4; i++) {
                if (!validateStep(i)) {
                    showNotification('Error', 'Please fill in all required fields', 'error');
                    // Go to the step with error
                    nextStep(i);
                    return;
                }
            }
            
            try {
                showLoading();
                
                // Prepare form data
                const formData = new FormData(form);
                
                // Convert cuisine array to string
                const cuisines = $('#cuisine_type').val();
                if (cuisines) {
                    formData.set('cuisine_type', cuisines.join(','));
                }
                
                // Prepare opening hours object
                const openingHours = {
                    weekday: formData.get('weekday_hours'),
                    saturday: formData.get('saturday_hours'),
                    sunday: formData.get('sunday_hours'),
                    display: `${formData.get('weekday_hours')} (Mon-Fri), ${formData.get('saturday_hours')} (Sat), ${formData.get('sunday_hours')} (Sun)`
                };
                formData.set('opening_hours', JSON.stringify(openingHours));
                
                // Remove temporary fields
                ['weekday_hours', 'saturday_hours', 'sunday_hours'].forEach(field => formData.delete(field));
                
                // Convert booleans
                ['is_active', 'is_accepting_orders', 'is_verified', 'is_featured', 'allow_preorder'].forEach(field => {
                    formData.set(field, formData.get(field) === 'on' ? 'true' : 'false');
                });
                
                // Send request
                const response = await fetch(API_BASE, {
                    method: 'POST',
                    headers: {
                        'Authorization': `Bearer ${getToken()}`
                    },
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showNotification('Success', 'Restaurant created successfully!', 'success');
                    
                    // Redirect to restaurants page after delay
                    setTimeout(() => {
                        window.location.href = '/manager/restaurants';
                    }, 2000);
                } else {
                    showNotification('Error', data.error || 'Failed to create restaurant', 'error');
                }
            } catch (error) {
                console.error('Error creating restaurant:', error);
                showNotification('Error', 'Failed to create restaurant: ' + error.message, 'error');
            } finally {
                hideLoading();
            }
        }
        
        // Get JWT token
        function getToken() {
            // This should be implemented based on your authentication system
            // For example, from localStorage or cookies
            return localStorage.getItem('manager_token') || '';
        }
    </script>
</body>
</html>